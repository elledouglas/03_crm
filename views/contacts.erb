

  <!--<h2>There are #{@contacts.count} contacts</h2>
  <ul>
    <% @contacts.each do |contact| %>
    <li><a href="contacts/<%=contact.id%>"><%= "#{contact.first_name}"%></a>
        <p class="point"><a href="contacts/<%=contact.id%>/edit"> Edit </a>| <a href="contacts/<%=contact.id%>/delete"> Delete </a></p>
      </li>
    <% end %>
  </ul> -->


<h2><%="There are #{@contacts.count} contacts" %></h2>


<ul>
<% @contacts.each do |contact| %>
<li>
<a href="/contacts">
<%= "#{contact.first_name}"%></a>
    <p class="point"><a href="contacts/<%=contact.id%>/edit"> Edit </a>| <a href="contacts/<%=contact.id%>/delete"> Delete </a></p>

</li>

<% end %>

</ul>
